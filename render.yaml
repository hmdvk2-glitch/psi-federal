services:
  - type: web
    name: psi-federal-simulation
    runtime: static
    buildCommand: npm install && npm run build
    publishPath: dist
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
